# Modern OWASP Top-50 Vulnerable Single-File Lab

Deliberately vulnerable local training app. Do not deploy.

## Run

```bash
npm install
npm start
```

Open http://127.0.0.1:3000

The main source is intentionally a single TypeScript file: `app.ts`.
